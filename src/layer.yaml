includes: ['layer:basic', 'layer:openstack', 'interface:juju-info', 'interface:influxdb-api']
options:
  basic:
    packages: [curl, apt-transport-https, golang, golang-github-influxdb-usage-client-dev, git]
